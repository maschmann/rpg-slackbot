{% if characters|length > 0 %}
    | Name | XP | Level |
{% for character in characters %}| {{ character.name }} | {{ character.experience }} | {{ character.level }} |
{% endfor %}
{% else %}
    No characters found
{% endif %}
